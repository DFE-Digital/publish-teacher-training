{
  "name": "teacher-training-api",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "build:css": "yarn run build:css:publish && yarn run build:css:find",
    "build:css:publish": "sass ./app/assets/stylesheets/application.scss ./app/assets/builds/application.css --no-source-map --load-path=node_modules --quiet-deps",
    "build:css:find": "sass ./app/assets/stylesheets/find_application.scss ./app/assets/builds/find_application.css --no-source-map --load-path=node_modules --quiet-deps",
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds",
    "pretest": "rm -rf .jest-cache",
    "standard:fix": "standard --fix",
    "standard": "standard",
    "test:ci": "jest --ci --runInBand --no-cache --coverage",
    "test": "jest"
  },
  "standard": {
    "envs": ["jest", "browser"],
    "globals": ["$", "google"]

  },
  "dependencies": {
    "@ministryofjustice/frontend": "^0.2.1",
    "accessible-autocomplete": "^2.0.4",
    "core-js": "^3.9.1",
    "govuk-frontend": "^3.9.1",
    "jquery": "^3.6.0",
    "set-value": "^4.0.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.18.9",
    "@testing-library/dom": "^8.16.0",
    "babel-jest": "^28.1.3",
    "babel-polyfill": "^6.26.0",
    "esbuild": "^0.14.43",
    "jest": "^28.1.3",
    "jest-environment-jsdom": "^28.1.3",
    "sass": "^1.52.3",
    "standard": "^17.0.0"
  }
}
