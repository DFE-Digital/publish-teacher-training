<% if render_navigation_bar?(@provider) %>
  <%= render(NavigationBar::View.new(
    items: navigation_items(@provider),
    current_path: request.path,
    current_user: @current_user,
  )) %>
<% end %>
