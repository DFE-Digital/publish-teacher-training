<%= render PageTitle::View.new(title: "support.allocations.index") %>

<h1 class="govuk-heading-l">Allocations</h1>

<%= render "support/menu" %>

<%= render PaginatedFilter::View.new(filters: @filters, collection: @allocations, filter_label: t("components.filter.allocations.text_search")) do %>
  <%= render "list", allocations: @allocations %>
<% end %>
