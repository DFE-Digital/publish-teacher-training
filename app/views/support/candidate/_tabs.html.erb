<%= render TabNavigation.new(items: [
  { name: t("support.candidates.details.details"), url: details_support_candidate_path(candidate), current: current_tab == :details },
  { name: t("support.candidates.details.saved_courses"), url: saved_courses_support_candidate_path(candidate), current: current_tab == :saved_courses },
  { name: t("support.candidates.details.notes"), url: notes_support_candidate_path(candidate), current: current_tab == :notes },
]) %>
