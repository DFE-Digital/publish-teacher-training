<%= govuk_notification_banner(
      title_text: banner.title,
      title_heading_level:,
      title_id:,
    ) do |notification_banner| %>
  <% notification_banner.with_heading(text: banner.heading) %>

  <%= body %>
<% end %>
