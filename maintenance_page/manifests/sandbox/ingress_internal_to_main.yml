apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: publish-sandbox.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: publish-sandbox.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: publish-sandbox
            port:
              number: 80
