apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging.api.publish-teacher-training-courses.service.gov.uk
spec:
  ingressClassName: nginx
  rules:
  - host: staging.api.publish-teacher-training-courses.service.gov.uk
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: publish-staging
            port:
              number: 80
