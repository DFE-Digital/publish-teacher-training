default: &default
  RAILS_SERVE_STATIC_FILES: true

review:
  <<: *default
  RAILS_ENV: review_aks
  RACK_ENV: review_aks

review_aks:
  <<: *default
  RAILS_ENV: review_aks
  RACK_ENV: review_aks

dv_review_aks:
  <<: *default
  RAILS_ENV: review_aks
  RACK_ENV: review_aks
